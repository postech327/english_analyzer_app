<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.english_analyzer_app">

    <!-- 인터넷 통신 허용 (Render API 호출 필수) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- (선택) 네트워크 상태 접근: 꼭 필요하진 않지만 디버깅에 도움 -->
    <!-- <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> -->

    <application
        android:name="${applicationName}"
        android:label="English Analyzer"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <!--
          로컬 개발에서 http://10.0.2.2:8000 같은 HTTP(비TLS)를 호출할 때만 필요
          Render(HTTPS)만 쓴다면 true가 아니어도 OK
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <!-- Flutter 런처 설정 -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <meta-data
                android:name="io.flutter.embedding.android.SplashScreenDrawable"
                android:resource="@drawable/launch_background" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Flutter 엔진 자동 초기화 -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
